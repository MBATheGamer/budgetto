server:
  servlet:
    context-path: "/api/v1"

spring:
  application:
    name: budgetto

  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}

  jpa:
    show-sql: true
